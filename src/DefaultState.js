const Immutable = require('immutable')

module.exports = Immutable.fromJS({
    activity: 'start',
    action: 'home',
    message: undefined,
    params: {},
    reason: 'normal',
    referer: {},
})
